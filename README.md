# AIVim

一个用Rust编写的现代化Vim编辑器，专注于轻量、快速和可扩展性。

![AIVim Screenshot](docs/images/screenshot.png)

## 特性

### 核心编辑功能
- ✅ **Vim兼容的编辑体验** - 熟悉的Normal/Insert/Visual模式
- ✅ **强大的移动命令** - `h/j/k/l`, `w/b/e`, `0/$/^`, `gg/G`, `Ctrl+D/U`
- ✅ **文本操作** - `yy`, `dd`, `p/P`, `x`, 撤销/重做
- ✅ **寄存器系统** - 支持 `"`, `0-9`, `a-z`, `A-Z` 等寄存器
- ✅ **搜索功能** - `/pattern`, `?pattern`, `n`, `N`
- ✅ **替换功能** - `:s/old/new`, `:s/old/new/g`, `:%s/old/new/g`
- ✅ **文件操作** - `:w`, `:q`, `:wq`, `:q!`

### 现代化特性
- 🚀 **Rust实现** - 内存安全，性能卓越
- 🎯 **LSP支持** (计划中) - 语言服务器协议支持
- 🔧 **插件系统** (计划中) - Lua插件支持
- 🎨 **主题系统** (计划中) - 可自定义配色方案

## 安装

### 系统要求

#### 硬件环境
- **CPU**: x86_64 或 ARM64 架构
- **内存**: 最低 256MB RAM，推荐 512MB+
- **磁盘**: 约 50MB 安装空间
- **终端**: 支持ANSI转义序列的终端（所有现代终端）

#### 操作系统
- ✅ macOS 10.14+
- ✅ Linux (内核 3.2+)
- ✅ Windows 10/11 (通过WSL2或Git Bash)

#### 依赖
- Rust 1.70+ (从源码编译时)
- Git (克隆仓库)

### 从源码安装

```bash
# 克隆仓库
git clone https://github.com/yourusername/aivim.git
cd aivim

# 编译发布版本
cargo build --release

# 安装到系统 (可选)
cargo install --path .
```

编译完成后，可执行文件位于：
- **macOS/Linux**: `./target/release/aivim`
- **Windows**: `.\target\release\aivim.exe`

### 快速开始

```bash
# 打开文件
./target/release/aivim filename.txt

# 创建新文件
./target/release/aivim newfile.txt

# 查看帮助
./target/release/aivim --help
```

## 使用指南

### 基本操作

| 命令 | 说明 |
|------|------|
| `i` | 在光标前进入插入模式 |
| `a` | 在光标后进入插入模式 |
| `Esc` | 返回Normal模式 |
| `:w` | 保存文件 |
| `:q` | 退出 |
| `:wq` | 保存并退出 |
| `:q!` | 强制退出不保存 |

### 移动命令

| 命令 | 说明 |
|------|------|
| `h/j/k/l` | 左/下/上/右 |
| `w` | 下一个单词开头（不跨行） |
| `b` | 上一个单词开头（不跨行） |
| `e` | 单词结尾 |
| `0` | 行首 |
| `^` | 第一个非空字符 |
| `$` | 行尾 |
| `gg` | 文件开头 |
| `G` | 文件结尾 |
| `Ctrl+D` | 向下翻半页 |
| `Ctrl+U` | 向上翻半页 |

### 编辑命令

| 命令 | 说明 |
|------|------|
| `x` | 删除字符 |
| `dd` | 删除行 |
| `yy` | 复制行 |
| `p` | 在光标后粘贴 |
| `P` | 在光标前粘贴 |
| `u` | 撤销 |
| `Ctrl+R` | 重做 |

### 搜索和替换

| 命令 | 说明 |
|------|------|
| `/pattern` | 向前搜索 |
| `?pattern` | 向后搜索 |
| `n` | 下一个匹配 |
| `N` | 上一个匹配 |
| `:s/old/new` | 替换当前行第一个 |
| `:s/old/new/g` | 替换当前行所有 |
| `:%s/old/new/g` | 替换整个文件 |

### 寄存器

| 命令 | 说明 |
|------|------|
| `"ayy` | 复制到寄存器a |
| `"ap` | 从寄存器a粘贴 |
| `"0p` | 从数字寄存器0粘贴 |

## 项目结构

```
aivim/
├── Cargo.toml              # 工作区配置
├── crates/
│   ├── aivim-core/         # 核心编辑引擎
│   │   ├── src/
│   │   │   ├── buffer.rs   # 文本缓冲区
│   │   │   ├── cursor.rs   # 光标管理
│   │   │   ├── editor.rs   # 编辑器状态
│   │   │   ├── mode.rs     # 编辑模式
│   │   │   ├── motion.rs   # 移动命令
│   │   │   ├── register.rs # 寄存器系统
│   │   │   ├── search.rs   # 搜索功能
│   │   │   └── replace.rs  # 替换功能
│   │   └── Cargo.toml
│   └── aivim-tui/          # 终端UI
│       ├── src/
│       │   ├── app.rs      # 应用主循环
│       │   ├── ui.rs       # UI渲染
│       │   └── main.rs
│       └── Cargo.toml
├── docs/
│   ├── design.md           # 设计文档
│   └── devlogs/            # 开发日志
└── README.md
```

## 开发

### 构建

```bash
# 调试构建
cargo build

# 发布构建
cargo build --release

# 运行测试
cargo test

# 运行示例
cargo run --example test_search -p aivim-core
```

### 调试

```bash
# 带日志运行
RUST_LOG=debug cargo run

# 使用示例文件测试
cargo run --release -- test_file.txt
```

## 路线图

### 阶段一：MVP ✅
- [x] 基础编辑功能
- [x] 文件操作
- [x] 光标移动
- [x] 撤销/重做

### 阶段二：基础功能 ✅
- [x] 寄存器系统
- [x] 搜索功能
- [x] 基础替换
- [x] 更多移动命令

### 阶段三：高级功能 (进行中)
- [ ] 文本对象 (`aw`, `iw`)
- [ ] Visual模式
- [ ] 多文件编辑
- [ ] 配置系统

### 阶段四：现代化 (计划中)
- [ ] LSP支持
- [ ] 语法高亮
- [ ] 插件系统 (Lua)
- [ ] 主题系统

## 贡献

欢迎贡献！请阅读 [CONTRIBUTING.md](CONTRIBUTING.md) 了解如何参与。

### 提交Issue

如果你发现了bug或有功能建议，请提交Issue：
1. 描述问题或建议
2. 提供复现步骤（如果是bug）
3. 说明期望的行为

### 提交PR

1. Fork本仓库
2. 创建特性分支 (`git checkout -b feature/amazing-feature`)
3. 提交更改 (`git commit -m 'Add amazing feature'`)
4. 推送到分支 (`git push origin feature/amazing-feature`)
5. 创建Pull Request

## 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

## 致谢

- 感谢Vim的创造者Bram Moolenaar，为我们提供了如此优秀的编辑器范式
- 感谢Rust社区提供的优秀工具和库
- 感谢所有贡献者和测试者

## 联系方式

- 项目主页: https://github.com/yourusername/aivim
- Issue追踪: https://github.com/yourusername/aivim/issues
- 讨论区: https://github.com/yourusername/aivim/discussions

---

**AIVim** - 用Rust重新想象Vim体验 🦀✨
